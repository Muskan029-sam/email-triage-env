name = "email-triage-env"
version = "0.1.0"
description = "Environment for spam filtering, urgency prioritization, and reply suggestion"
authors = [
    { name = "Muskan", email = "anshika6d5.hts21@gmail.com" }
]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
